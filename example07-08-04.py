class Ball:
    def __init__(self, x, y, dx, dy, color):
        self.x = x
        self.y = y
        self.dx = dx
        self.dy = dy
        self.color = color
    def test(self)
        print(self.x)
        print(self.y)

b = Ball(400, 300, 1, ,1 "red")